<odoo>
    <data noupdate="1">
        <!-- 1. CATEGORÍA -->
        <record id="module_category_real_estate_brokerage" model="ir.module.category">
            <field name="name">Real Estate</field>
            <field name="sequence">10</field>
        </record>

        <!-- 2. GRUPOS -->
        <record id="estate_group_user" model="res.groups">
            <field name="name">Agent</field>
            <field name="category_id" ref="module_category_real_estate_brokerage"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <record id="estate_group_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_real_estate_brokerage"/>
            <field name="implied_ids" eval="[(4, ref('estate_group_user'))]"/>
        </record>

        <!-- 3. REGLA DE REGISTRO (Esto es lo que faltaba separado) -->
        <record id="estate_rule_agent_personal_properties" model="ir.rule">
            <field name="name">Agente: Solo propiedades propias</field>
            <field name="model_id" ref="model_real_estate"/>
            <field name="groups" eval="[(4, ref('estate_group_user'))]"/>
            <field name="domain_force">['|', ('salesperson_id', '=', False), ('salesperson_id', '=', user.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

                <!-- REGLA PARA EL MANAGER: Verlo TODO -->
        <record id="estate_rule_manager_all_properties" model="ir.rule">
            <field name="name">Manager: Ver todas las propiedades</field>
            <field name="model_id" ref="model_real_estate"/>
            <field name="groups" eval="[(4, ref('estate_group_manager'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field> <!-- [(1, '=', 1)] significa SIEMPRE VERDADERO (acceso total) -->
        </record>
                <!-- Regla Multi-compañía -->
        <record id="real_estate_rule_multi_company" model="ir.rule">
            <field name="name">Real Estate Multi-Company</field>
            <field name="model_id" ref="model_real_estate"/>
            <field name="domain_force">
                ['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            </field>
        </record>
    </data>
</odoo>
